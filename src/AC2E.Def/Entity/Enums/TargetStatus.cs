namespace AC2E.Def {

    // Enum TargetStatus
    public enum TargetStatus : uint {
        UNDEF,
        OK,
        EXIT_WORLD,
        TELEPORTED,
        CONTAINED,
        PARENTED,
        TIMED_OUT,
    }
}
