namespace AC2E.Def {

    // Const NET_QUEUE_*
    public enum NetQueue : ushort {
        INVALID,
        EVENT,
        CONTROL,
        WEENIE,
        LOGON,
        DATABASE,
        SECURECONTROL,
        SECUREWEENIE,
        SECURELOGON,
    }
}
