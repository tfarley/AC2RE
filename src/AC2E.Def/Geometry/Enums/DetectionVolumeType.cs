namespace AC2E.Def {

    // Enum DetectionVolumeType
    public enum DetectionVolumeType : uint {
        UNDEFINED = 0,
        RAY = 1,
        BOUND_VOLUME = 2,
        SPHERE_CONE = 3,
        SPHERE = 4,
        SPHERE_PLANE_CONE = 5,
        BOUND_BOX = 6,
        CYLINDER = 7,
    }
}
