namespace AC2E.Def {

    // Enum LandDefs::PalType
    public enum PalType : uint {
        SW_TERRAIN = 0,
        SE_TERRAIN = 1,
        NE_TERRAIN = 2,
        NW_TERRAIN = 3,
        ROAD = 4,
    }
}
