namespace AC2E.Def.Enums {

    // Enum LandscapeHeightRelativeType::Type
    public enum LandscapeHeightRelativeType : uint {
        LAND_HEIGHT = 0,
        WATER_HEIGHT = 1,
        ZERO_HEIGHT = 2,
        MAX_BLOCK_HEIGHT = 3,
        MIN_BLOCK_HEIGHT = 4,
        AVE_BLOCK_HEIGHT = 5,
        MAX_CELL_HEIGHT = 6,
        MIN_CELL_HEIGHT = 7,
        AVE_CELL_HEIGHT = 8,
        FRAME = 9,
        INVALID = 10,
    }
}
