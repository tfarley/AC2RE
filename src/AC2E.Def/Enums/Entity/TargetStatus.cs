namespace AC2E.Def.Enums {

    // Enum TargetStatus
    public enum TargetStatus : uint {
        UNDEF = 0,
        OK = 1,
        EXIT_WORLD = 2,
        TELEPORTED = 3,
        CONTAINED = 4,
        PARENTED = 5,
        TIMED_OUT = 6,
    }
}
