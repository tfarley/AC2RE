namespace AC2E.Def {

    // Enum LandscapeHeightRelativeType::Type
    public enum LandscapeHeightRelativeType : uint {
        LAND_HEIGHT,
        WATER_HEIGHT,
        ZERO_HEIGHT,
        MAX_BLOCK_HEIGHT,
        MIN_BLOCK_HEIGHT,
        AVE_BLOCK_HEIGHT,
        MAX_CELL_HEIGHT,
        MIN_CELL_HEIGHT,
        AVE_CELL_HEIGHT,
        FRAME,
        INVALID,
    }
}
