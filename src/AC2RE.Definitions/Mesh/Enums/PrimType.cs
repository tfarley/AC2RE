namespace AC2RE.Definitions {

    // Enum PrimType
    public enum PrimType : uint {
        POINTLIST = 1,
        LINELIST = 2,
        LINESTRIP = 3,
        TRIANGLELIST = 4,
        TRIANGLESTRIP = 5,
        TRIANGLEFAN = 6,

        INVALID = 0x7FFFFFFF,
    }
}
