namespace AC2RE.Definitions;

// Enum ShadowType
public enum ShadowType : uint {
    NONE = 0, // SHADOWTYPE_NONE
    STATIC = 1, // SHADOWTYPE_STATIC
    VOLUMETRIC = 2, // SHADOWTYPE_VOLUMETRIC

    INVALID = 0x7FFFFFFF, // PRIMTYPE_INVALID
}
