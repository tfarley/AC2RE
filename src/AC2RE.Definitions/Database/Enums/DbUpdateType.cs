namespace AC2RE.Definitions {

    // Enum DBUpdateType::Type
    public enum DbUpdateType : uint {
        Skip, // Skip + None
        Patch, // Patch
        Dynamic, // Dynamic
        Zone, // Zone
    }
}
