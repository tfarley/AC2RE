namespace AC2RE.Definitions {

    // Enum DetectionVolumeType
    public enum DetectionVolumeType : uint {
        UNDEFINED,
        RAY,
        BOUND_VOLUME,
        SPHERE_CONE,
        SPHERE,
        SPHERE_PLANE_CONE,
        BOUND_BOX,
        CYLINDER,
    }
}
