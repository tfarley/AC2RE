namespace AC2RE.Definitions {

    // Enum Sidedness
    public enum Sidedness : uint {
        POSITIVE, // POSITIVE
        NEGATIVE, // NEGATIVE
        IN_PLANE, // IN_PLANE
        CROSSING, // CROSSING
    }
}
