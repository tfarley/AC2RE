namespace AC2RE.Definitions {

    // Const - globals
    public enum LootingMethod : uint {
        UNDEF,
        SHARING, // SHARING
        ROTATING, // ROTATING
        ROTATINGGROUP, // ROTATINGGROUP
        RANDOM, // RANDOM
    }
}
