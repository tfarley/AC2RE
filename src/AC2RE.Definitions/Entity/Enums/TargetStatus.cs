namespace AC2RE.Definitions {

    // Enum TargetStatus
    public enum TargetStatus : uint {
        UNDEF,
        OK,
        EXIT_WORLD,
        TELEPORTED,
        CONTAINED,
        PARENTED,
        TIMED_OUT,
    }
}
