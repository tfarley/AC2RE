namespace AC2RE.Definitions {

    // Enum RMDataType
    public enum RMDataType : uint {
        WAVEFORM = 1000,
        COLOR = 2000,
        TEXTURE = 3000,
        BOOL = 4000,
        INVALID = 0x7FFFFFFF,
    }
}
