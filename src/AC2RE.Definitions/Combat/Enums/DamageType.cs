namespace AC2RE.Definitions {

    // Dat file 2300010F
    public enum DamageType : uint {
        Undef = 0,
        Nature = 1, // Nature
        Decay = 2, // Decay
        Martial = 3, // Martial
        Arcane = 4, // Arcane
    }
}
