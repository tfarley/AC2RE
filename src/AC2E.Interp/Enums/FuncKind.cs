namespace AC2E.Interp {

    // Enum tagFUNCKIND
    public enum FuncKind : uint {
        VIRTUAL = 0,
        PUREVIRTUAL = 1,
        NONVIRTUAL = 2,
        STATIC = 3,
        DISPATCH = 4,
    }
}
