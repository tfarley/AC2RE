namespace AC2E.Interp {

    // Enum tagDESCKIND
    public enum DescKind : uint {
        NONE = 0,
        FUNCDESC = 1,
        VARDESC = 2,
        TYPECOMP = 3,
        IMPLICITAPPOBJ = 4,
    }
}
